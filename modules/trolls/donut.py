import subprocess

def donut_smp():
    """Visual 'Donut' terminal overlay or browser trigger."""
    # Point to the classic a1k0n ASCII donut
    subprocess.Popen(["start", "https://www.a1k0n.net/js/donut.html"], shell=True)
